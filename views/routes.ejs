<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>เส้นทางทั้งหมด</title>
  <script src="https://cdn.tailwindcss.com"></script>
<style>
    body {
      background: url('https://i.pinimg.com/736x/9c/04/11/9c04111ba071591f81a455f8e897c5df.jpg')
        no-repeat center center fixed;
      background-size: cover;
      font-family: 'Prompt', sans-serif;
      color: #111; /* ตัวอักษรสีดำ */
    }

    .glass {
      background: rgba(255, 255, 255, 0.8);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.4);
    }

    select, input, button {
      color: #111 !important;
    }
  </style>
</head>
<body class="min-h-screen flex flex-col items-center justify-center bg-gray-100 text-black">
  <div class="bg-white/80 rounded-2xl p-8 shadow-2xl max-w-3xl w-full mx-4">
    <h1 class="text-3xl font-bold mb-6 text-center">เส้นทางรถแดงทั้งหมด</h1>
    <table class="w-full text-sm md:text-base border-collapse border border-gray-300">
      <thead>
        <tr class="bg-gray-200 border-b border-gray-300">
          <th class="py-2 px-2">สาย</th>
          <th>เส้นทาง</th>
          <th>ต้นทาง</th>
          <th>ปลายทาง</th>
          <th class="text-center">ดูจุดจอด</th>
        </tr>
      </thead>
      <tbody>
        <% routes.forEach(r => { %>
        <tr class="hover:bg-gray-100 border-b">
          <td class="py-2 px-2 font-semibold"><%= r.route_number %></td>
          <td><%= r.route_name %></td>
          <td><%= r.start_point %></td>
          <td><%= r.end_point %></td>
          <td class="text-center">
            <a href="/stops/<%= r.route_id %>" class="text-blue-600 hover:underline">ดู</a>
          </td>
        </tr>
        <% }); %>
      </tbody>
    </table>

    <div class="text-center mt-6">
      <a href="/" class="bg-gradient-to-r from-pink-400 to-red-500 px-5 py-2 rounded-lg font-semibold text-white hover:opacity-90">⬅ กลับไปหน้าค้นหา</a>
    </div>
  </div>
</body>
</html>
